---
id: CT-002
title: Add injection-safe command value channels
status: blocked
priority: high
task_type: task
tags:
    - ready-for-agent
    - review-followup
    - security
    - breaking-api
acceptance_criteria:
    - 'Blocking edge: CT-001 must be done before this task starts.'
    - A public structured execution path supports caller-controlled values without shell re-parsing them.
    - Regression tests prove that quotes, command substitutions, separators, newlines, and option-like values remain data.
    - Secrets are masked consistently in returned errors, captured output, callbacks, and persisted history on the new path.
    - Legacy raw shell interpolation is explicitly unsafe or deprecated according to the approved trust model, with migration guidance.
    - Existing structured and shell execution tests remain green.
verification_plan:
    - Run focused command, variable, security, and history tests including race mode.
    - Run go vet and gopls diagnostics for changed packages.
    - Run the full Go test and race suites.
created_at: "2026-08-28T07:54:52.821215Z"
updated_at: "2026-08-28T07:58:15.718286Z"
---

## Acceptance Criteria

- Blocking edge: CT-001 must be done before this task starts.
- A public structured execution path supports caller-controlled values without shell re-parsing them.
- Regression tests prove that quotes, command substitutions, separators, newlines, and option-like values remain data.
- Secrets are masked consistently in returned errors, captured output, callbacks, and persisted history on the new path.
- Legacy raw shell interpolation is explicitly unsafe or deprecated according to the approved trust model, with migration guidance.
- Existing structured and shell execution tests remain green.

## Verification Plan

1. Run focused command, variable, security, and history tests including race mode.
2. Run go vet and gopls diagnostics for changed packages.
3. Run the full Go test and race suites.
