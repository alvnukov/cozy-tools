---
id: CT-004
title: Make CommitOwned transactional across processes
status: todo
priority: high
task_type: task
tags:
    - ready-for-agent
    - review-followup
    - security
    - git
acceptance_criteria:
    - The operation uses an interprocess repository lock or an equivalent serialization mechanism.
    - Staging and validation occur in an isolated index or an equivalently rollback-safe transaction.
    - A commit cannot include a path outside the normalized owned set, including when another process changes the real index concurrently.
    - Every failure path preserves the original index and worktree state.
    - Regression tests cover competing writers, pre-existing staged changes, partial staging failure, ignored files, pathspec magic, and platform path forms.
verification_plan:
    - Run focused Git tests repeatedly and under the race detector.
    - Run process-level concurrency regression tests.
    - Run the full Go test suite on supported host platforms.
created_at: "2026-08-28T07:54:52.823973Z"
updated_at: "2026-08-28T07:55:44.491461Z"
---

## Body

## What to build
Make owned-file commits atomic with respect to the repository index and competing helper processes. A successful call must commit exactly the declared owned set; any validation, staging, or commit failure must leave the caller's original index and worktree state intact.

## Blocked by
None — can start immediately.

## Acceptance Criteria

- The operation uses an interprocess repository lock or an equivalent serialization mechanism.
- Staging and validation occur in an isolated index or an equivalently rollback-safe transaction.
- A commit cannot include a path outside the normalized owned set, including when another process changes the real index concurrently.
- Every failure path preserves the original index and worktree state.
- Regression tests cover competing writers, pre-existing staged changes, partial staging failure, ignored files, pathspec magic, and platform path forms.

## Verification Plan

1. Run focused Git tests repeatedly and under the race detector.
2. Run process-level concurrency regression tests.
3. Run the full Go test suite on supported host platforms.
