---
id: CT-005
title: Capture ordered combined output as a bounded tail
status: todo
priority: medium
task_type: task
tags:
    - ready-for-agent
    - review-followup
    - command
    - api
acceptance_criteria:
    - Retained output contains the latest bytes rather than the earliest bytes after overflow.
    - Interleaved stdout and stderr preserve observable order while callers can still identify the source stream.
    - The total retained payload never exceeds one documented output budget.
    - The output hash covers the complete redacted output even when retention truncates it.
    - Callbacks, history, failure markers, and public result fields have documented and mutually consistent semantics.
    - Regression tests cover large late failures, UTF-8 boundaries, interleaving, cancellation, and output from multiple writers.
verification_plan:
    - Run focused command output and history tests under the race detector.
    - Run memory-bound and truncation regression tests.
    - Run the full Go test suite and gopls diagnostics.
created_at: "2026-08-28T07:54:52.824853Z"
updated_at: "2026-08-28T07:55:44.492527Z"
---

## Body

## What to build
Replace separate prefix retention with one ordered, bounded command-output tail. Preserve stdout/stderr identity while retaining cross-stream order, enforce one total memory budget, and compute evidence from the complete redacted stream rather than only retained bytes.

## Blocked by
None — can start immediately.

## Acceptance Criteria

- Retained output contains the latest bytes rather than the earliest bytes after overflow.
- Interleaved stdout and stderr preserve observable order while callers can still identify the source stream.
- The total retained payload never exceeds one documented output budget.
- The output hash covers the complete redacted output even when retention truncates it.
- Callbacks, history, failure markers, and public result fields have documented and mutually consistent semantics.
- Regression tests cover large late failures, UTF-8 boundaries, interleaving, cancellation, and output from multiple writers.

## Verification Plan

1. Run focused command output and history tests under the race detector.
2. Run memory-bound and truncation regression tests.
3. Run the full Go test suite and gopls diagnostics.
